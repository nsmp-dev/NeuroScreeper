module.exports = {
    // sets whether to print or not to clean up the console
    run: function () {
        // check if we can make a power creep

        // spawn it if needed

        // upgrade it if needed

        // run it if we can
    },
};