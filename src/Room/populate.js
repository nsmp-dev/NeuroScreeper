const POPULATION_GOALS = [];

const CREEP_TEMPLATES = {
	bootstrapper: {
		base: [],
		base_cost: 100,
		extras: [],
		extras_cost: 100,
	},
};

Room.prototype.populate = function () {
	//while new creeps are needed and enough energy is available
		//spawn new creep
};